# --- Python / virtual environment ---
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# --- secrets: never commit these ---
.env
*.env

# holds forge.db (SQLite) and uploaded CVs/videos — regenerated by seed-demo
instance/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- if/when the Capacitor mobile wrapper is added alongside this repo ---
node_modules/
android/app/build/
android/build/
android/.gradle/
android/local.properties      # contains a local absolute SDK path, machine-specific
android/*.iml
ios/App/Pods/
.~lock.*#

# Autonomous agent artifacts
.project_inspection_*
**inspection*.tar.gz
*.agent-backup

# Local agent runtime state
.sandbox-state/
